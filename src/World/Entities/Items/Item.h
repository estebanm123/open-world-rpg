#pragma once

#include <memory>
#include "../Collidables/Props/Prop.h"

// Physical representation in the world is represented by a non-decor Prop
class Item {
public:
    // todo: extract Prop (when Humanoid drops)
    // refactor PropInitializer to allow variant bypass

};
