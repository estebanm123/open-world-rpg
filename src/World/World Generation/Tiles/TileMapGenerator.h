#pragma once

#include <vector>
#include <memory>
#include <SFML/Graphics.hpp>
#include "TileMap.h"

class TileMapGenerator {
public:
    static TileMap::Tiles generate(sf::Vector2f pos);

private:

};




